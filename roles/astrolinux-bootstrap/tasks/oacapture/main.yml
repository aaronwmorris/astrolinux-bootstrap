---

- include: "oacapture/install/main.yml"


- name: Install precompiled oacapture packages
  package:
    name: "{{ oacapture_package_urls[dist_ver][ansible_facts['architecture']] }}"
    state: present
  become: yes

