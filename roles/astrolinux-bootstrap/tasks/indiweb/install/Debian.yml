---


- name: Update repositories cache
  become: yes
  apt:
    update_cache: yes


- name: Install indiweb development packages
  become: yes
  package:
    name: "{{ indiweb_packages }}"
    state: present

